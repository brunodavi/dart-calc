name: dart_calc
description: A sample command-line application to simples calcs.
version: 1.0.0
repository: https://github.com/brunodavi/dart-calc

executables:
  dart-calc: bin/dart_calc.dart

environment:
  sdk: ^3.6.1

# Add regular dependencies here.
dependencies:
  # path: ^1.8.0

dev_dependencies:
  lints: ^5.0.0
  test: ^1.24.0
